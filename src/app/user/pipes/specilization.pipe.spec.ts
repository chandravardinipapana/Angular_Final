import { SpecilizationPipe } from './specilization.pipe';

describe('SpecilizationPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecilizationPipe();
    expect(pipe).toBeTruthy();
  });
});
